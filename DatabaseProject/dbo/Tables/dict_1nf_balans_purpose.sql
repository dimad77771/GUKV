CREATE TABLE [dbo].[dict_1nf_balans_purpose] (
    [id]         INT           NOT NULL,
    [group_code] INT           NULL,
    [name]       VARCHAR (160) NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

