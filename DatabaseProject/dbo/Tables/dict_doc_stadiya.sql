CREATE TABLE [dbo].[dict_doc_stadiya] (
    [id]          INT           NOT NULL,
    [name]        VARCHAR (60)  NULL,
    [modified_by] VARCHAR (128) NULL,
    [modify_date] DATE          NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

