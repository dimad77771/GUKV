CREATE TABLE [dbo].[dict_balans_purpose_group] (
    [id]          INT           NOT NULL,
    [name]        VARCHAR (160) NULL,
    [modified_by] VARCHAR (128) NULL,
    [modify_date] DATE          NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

