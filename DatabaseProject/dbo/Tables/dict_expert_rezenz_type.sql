CREATE TABLE [dbo].[dict_expert_rezenz_type] (
    [id]        INT           NOT NULL,
    [name]      VARCHAR (100) NULL,
    [color_rgb] INT           NOT NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

