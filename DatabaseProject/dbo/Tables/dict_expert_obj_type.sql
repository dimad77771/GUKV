CREATE TABLE [dbo].[dict_expert_obj_type] (
    [id]         INT           NOT NULL,
    [name]       VARCHAR (100) NULL,
    [short_name] VARCHAR (50)  NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

