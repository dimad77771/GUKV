CREATE TABLE [dbo].[dict_stan_recieve] (
    [stan_recieve_id]   INT            NOT NULL,
    [step_ord]          INT            NOT NULL,
    [stan_recieve_name] VARCHAR (1000) NOT NULL,
    PRIMARY KEY CLUSTERED ([stan_recieve_id] ASC)
);

